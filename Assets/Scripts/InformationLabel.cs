using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class InformationLabel : MonoBehaviour {

    public Text CCount;
    public Text NCount;
    public Text HCount;
    public Text SCount;
    public Text OCount;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
//        OCount.text = "#Oxygen: " + PDB_Parser1.OCounter.ToString();
//        SCount.text = "#Sulfur: " + PDB_Parser1.SCounter.ToString();
//        HCount.text = "#Hydrogen: " + PDB_Parser1.HCounter.ToString();
//        NCount.text = "#Nitrogen: " + PDB_Parser1.NCounter.ToString();
//        CCount.text = "#Carbon: " + PDB_Parser1.CCounter.ToString();
    }
}
